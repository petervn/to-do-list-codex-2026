# Full-Stack To-Do List (React + TypeScript + Express)

## Quick start
```bash
npm install        # installs both client and server deps
npm run dev        # runs Vite dev server (client) and Express (server)
